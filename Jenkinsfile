pipeline {
    agent any
    stages {        
        stage('Build') { 
            steps {
        	    sh 'cd /var/lib/jenkins/workspace/aws-ec2-jenkins-tuto2 && npm install'
            }
        }
    }
}
